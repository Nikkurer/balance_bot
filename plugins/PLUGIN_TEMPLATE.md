# Название сервиса (`имя_плагина`)

Кратко: для чего плагин, какой сервис мониторит.

## Как получить API-токен

1. …
2. …
3. Скопируйте токен в `config.yaml` → `plugin_config.api_token`.

### Важно

- …
- Токен не коммитьте в git; храните только в `config.yaml` (файл в `.gitignore`).

## Выбор API / региона (если применимо)

| Параметр | Значение | URL / поведение |
|----------|----------|-----------------|
| … | … | … |

Переопределение: `base_url` и/или другие поля в `plugin_config`.

## Пример конфигурации

```yaml
- name: my-service
  plugin: <имя_плагина>
  poll_interval_seconds: 3600
  balance_threshold: 100.0
  subscription_warn_days: 7
  plugin_config:
    api_token: "..."
    # ...
```

## Параметры `plugin_config`

| Поле | Обязательно | По умолчанию | Описание |
|------|-------------|--------------|----------|
| `api_token` | да | — | API-токен |
| … | … | … | … |

## Откуда берутся данные

| Поле бота | Источник API |
|-----------|--------------|
| `balance` | … |
| `subscription_end` | … (дата от сервиса, бот не считает сам) |
| `currency` | … |

## Частые проблемы

- **Ошибка авторизации** — …
- **Неверный регион / site** — …

# Образ из GitHub Container Registry (после Release → workflow Release).
# Имя в нижнем регистре: ghcr.io/<owner>/<repo>
BALANCE_BOT_IMAGE=ghcr.io/OWNER/REPO:1.0.0
# 1=true включить debug-логи, 0=false обычный режим
BALANCE_BOT_DEBUG=0

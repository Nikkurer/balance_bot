# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local config (may contain secrets)
config.yaml

# SQLite history (balance time series)
data/

# Локальные переменные для docker compose (образ GHCR)
.env
